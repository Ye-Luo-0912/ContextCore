---
description: ContextCore 活路径与下一阶段执行约定
alwaysApply: true
---

# ContextCore 活路径

- 现行调用链只认 `docs/LIVE_PATH.md`。不要从 `TODO.md` 后半、`docs/archive/`、`vector/`、`learning/` 推断 DI。
- 下一阶段按 `docs/RECALL_WIRING.md` **一次一个工作包**执行。复制该文件开头的提示词，换工作包编号。
- 产品：模型输入 = 工作集 + 搜索。忘掉靠工作集，找回靠搜索。准不准跟工具观察走，不跟固定乘数、`FinalScore`、标题 +50、Completed=0.9 走。
- 禁止：R46（WP-AC/AD）、打开 Adaptive Active、Learning/ONNX/默认 embedding、原型 materialize/Warm-Cold/admit、把选中 ID 写成 RequiredIds、把 Resident 写入 IWorkingMemoryService、按连字符拆实体词。
- 注释中文、无任务编号。只跑工作包写明的定向测试。不要自己 commit。
