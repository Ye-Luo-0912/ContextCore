# ContextCore 开发规则

## 注释规则

- 注释使用中文，不加标签前缀（不写 `// 目标：`、`// 说明：` 之类的冒号标签，直接写内容）。
- 注释中**禁止出现任务编号/编码**：不引用 R30A、WP-A、P1-5、R13.2 #1、#3、验收标准编号、用户澄清编号等任何外部编号。
- 注释中**不得引用无关的实体内容**：工作包编号、issue/澄清编号、验收条目编号等一律不写进注释。
- 保持简洁明了：只描述代码本身的意图、约束与行为，不写进度、来源、阶段编号。
- 例外：`TestCategory` 值、异常消息、断言消息、日志字符串、commit 消息中可保留业务编号。
